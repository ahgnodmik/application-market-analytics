# 🔧 404 에러 해결 가이드

## 문제

```
GET https://app-market-analytics.netlify.app/ 404 (Not Found)
```

Netlify가 Functions를 찾지 못하고 있습니다.

## 원인

Netlify Functions의 구조가 올바르지 않았습니다.

## 해결 방법

### 올바른 구조

```
netlify/functions/
└── server/
    ├── server.py          ← 함수 엔트리 포인트 (handler 변수 export)
    └── requirements.txt   ← 의존성
```

### 핵심 포인트

1. **디렉토리 이름 = 함수 이름**
   - `netlify/functions/server/` → 함수 이름: `server`

2. **파일 이름 = 디렉토리 이름**
   - `server/server.py` (권장)
   - 또는 `server/handler.py` (하지만 `handler` 변수 필요)

3. **Handler 변수 export**
   - `server.py` 또는 `handler.py`에서 `handler` 변수를 export해야 함

## 현재 구조

```
netlify/functions/
└── server/
    ├── server.py          ✅ (handler 변수 포함)
    └── requirements.txt   ✅
```

## netlify.toml 설정

```toml
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"  # server 디렉토리 이름과 일치
  status = 200
  force = true
```

## 확인 방법

### 1. Functions 배포 확인

Netlify 대시보드 → Functions 탭:
- `server` 함수가 목록에 있는지 확인
- 함수 상태가 정상인지 확인

### 2. Functions 로그 확인

Netlify 대시보드 → Functions → server → Logs:
- `[Server] ✅ FastAPI app imported successfully` 메시지 확인
- 에러 메시지 확인

### 3. 직접 Functions 엔드포인트 테스트

```
https://app-market-analytics.netlify.app/.netlify/functions/server/
```

이 URL이 작동한다면:
- ✅ Functions는 정상 작동
- ⚠️ 문제는 redirects 설정

이 URL도 404라면:
- ❌ Functions 자체가 배포되지 않음
- ❌ Functions 로그 확인 필요

## 변경사항 적용 완료

✅ Functions 구조를 `server/server.py`로 수정
✅ 모든 변경사항 푸시 완료

Netlify가 자동으로 다시 배포합니다.

## 다음 단계

1. **Netlify 대시보드 확인**
   - Functions 탭 → `server` 함수 존재 확인
   - Deploys 탭 → 최신 배포 상태 확인

2. **Functions 로그 확인**
   - Functions → server → Logs
   - `[Server] ✅` 메시지 확인

3. **직접 Functions 테스트**
   - `/.netlify/functions/server/health` 접속
   - 응답 확인

4. **메인 페이지 재확인**
   - `https://app-market-analytics.netlify.app/` 접속
   - 여전히 404라면 Functions 로그 공유 필요
