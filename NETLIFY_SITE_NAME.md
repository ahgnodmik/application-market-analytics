# 📝 Netlify 사이트 이름 변경 가이드

## 현재 상황
- Netlify 사이트 이름: `shotsmaker`
- 실제 프로젝트: **Application Market Analytics (앱 마켓 분석기)**

## 사이트 이름 변경 방법

### 방법 1: Netlify 대시보드에서 변경

1. **https://app.netlify.com** 접속
2. 사이트 선택 (`shotsmaker`)
3. **Site settings** → **General**
4. **Site details** 섹션에서:
   - **Site name** 필드 찾기
   - `application-market-analytics` 또는 원하는 이름으로 변경
   - 예: `app-market-analytics`, `market-analytics`, `android-market-analysis`
5. **Save** 클릭

### 방법 2: URL 변경 (사용자 정의 도메인)

- Netlify는 사이트 이름을 기반으로 자동 URL을 생성합니다
- 사이트 이름 변경 시 URL도 변경됩니다
- 예: `shotsmaker.netlify.app` → `application-market-analytics.netlify.app`

## 추천 사이트 이름

다음 중 선택하세요:

1. `application-market-analytics` (전체 이름)
2. `app-market-analytics` (짧은 버전)
3. `market-analytics` (더 짧음)
4. `android-market-analysis` (명확한 설명)

## 참고

- 사이트 이름 변경은 즉시 적용됩니다
- 기존 URL은 리다이렉트될 수 있지만, 새 URL 사용을 권장합니다
- 환경 변수와 다른 설정은 유지됩니다


