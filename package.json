{
  "name": "application-market-analytics",
  "version": "1.0.0",
  "description": "안드로이드 마켓 분석기 - 구현 난이도가 낮고 시장성이 검증된 앱 타입 추출 서비스",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "build": "echo 'No build step needed - Netlify will install dependencies from requirements.txt'",
    "netlify:dev": "netlify dev",
    "netlify:deploy": "netlify deploy",
    "netlify:deploy:prod": "netlify deploy --prod"
  },
  "keywords": [
    "analytics",
    "android",
    "market",
    "netlify",
    "fastapi",
    "python"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "python": "3.9",
    "node": ">=18.0.0"
  }
}

